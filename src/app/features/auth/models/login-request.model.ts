export interface LoginRequest {
  email: string;          // Two properties needed by the API to 
                          // perform login request from the angular 
                          // application to the API
  password: string;
}
