export interface AddCategoryRequest {
  urlHandle: string;
  name: string; // same as CreateCategoryRequestDto properties
}//for add-category.component.html form input
  // use this AddCategoryRequest object inside 
  //the ts file of this component(add-category)