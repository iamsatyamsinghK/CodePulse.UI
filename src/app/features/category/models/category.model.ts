export interface Category {
  id: string;
  name: string;
  urlHandle: string;
}
                  // same as response of GetAllCategories(),
                  //GetCategoryById(), EditCategory()
                  // of CategoriesController of API
                  //i.e CategoryDto